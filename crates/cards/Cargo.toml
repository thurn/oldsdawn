[package]
name = "cards"
version = "0.0.0"
description = "Card definitions"
license = "Apache-2.0"
edition = "2021"

[lib]
doctest = false
test = false

[dependencies]
linkme = "0.2.10"

data = { path = "../data", version = "0.0.0" }
rules = { path = "../rules", version = "0.0.0" }
